import { Metadata } from "next";

export const metadata: Metadata = { title: "광고 매칭" };

export default function SponsorshipLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
